<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/artl_ttl_txt_view"
        style="@style/list_text_view"
        android:textAppearance="@android:style/TextAppearance.Large"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/artl_auth_txt_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Title" />

    <TextView
        android:id="@+id/artl_auth_txt_view"
        style="@style/list_text_view"
        android:textAppearance="@android:style/TextAppearance.Small"
        android:textColor="@android:color/black"
        app:layout_constraintBottom_toTopOf="@+id/artl_sec_txt_view"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Author" />

    <TextView
        android:id="@+id/artl_sec_txt_view"
        style="@style/list_text_view"
        android:textAppearance="@android:style/TextAppearance.Small"
        android:textColor="@android:color/black"
        app:layout_constraintBottom_toTopOf="@+id/artl_date_txt_view"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Section" />

    <TextView
        android:id="@+id/artl_date_txt_view"
        style="@style/list_text_view"
        android:textAppearance="@android:style/TextAppearance.Small"
        android:textColor="@android:color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Date" />

</android.support.constraint.ConstraintLayout>