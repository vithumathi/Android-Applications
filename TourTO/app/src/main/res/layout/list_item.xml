<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#F5F5F5"
    android:minHeight="@dimen/list_item_height"
    android:orientation="vertical"
    android:padding="16dp">

    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="120dp"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#F5F5F5"
        android:orientation="vertical"
        android:paddingLeft="10dp"
        android:paddingRight="10dp">

        <TextView
            android:id="@+id/attraction_name_text_view"
            style="@style/list_item_style"
            android:textAppearance="@android:style/TextAppearance.Medium"
            android:textStyle="bold"
            tools:text="@string/list_attr_name" />

        <TextView
            android:id="@+id/attraction_disc_text_view"
            style="@style/list_item_style"
            android:textAppearance="@android:style/TextAppearance.Small"
            tools:text="@string/list_attr_disc" />

        <TextView
            android:id="@+id/attraction_addr_text_view"
            style="@style/list_item_style"
            android:textAppearance="@android:style/TextAppearance.Small"
            tools:text="@string/list_attr_addr" />
    </LinearLayout>

</LinearLayout>